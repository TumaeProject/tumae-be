# Tumae API - 코딩 과외 매칭 플랫폼 백엔드

FastAPI 기반의 코딩 과외 매칭 플랫폼 백엔드 시스템입니다. 학생과 튜터를 연결하고, 커뮤니티, 쪽지함, 이력서 관리 등의 기능을 제공합니다.

**main 브랜치**는 코드를 합치는 협업용도로 사용하였고, **최종 main.py 저장과 배포는 jeonga 브랜치**에서 진행했습니다.

---

## 🚀 빠른 시작

### **배포된 서비스 (설치 없이 바로 테스트) ⭐**

- **API 문서**: https://tumae-jeonga.onrender.com/docs
- **사용 방법**: 아래의 [완전한 API 테스트 가이드](#-완전한-api-테스트-가이드)를 따라하세요

---

## 📋 목차
- [프로젝트 구조](#-프로젝트-구조)
- [주요 기능](#-주요-기능)
- [기술 스택](#-기술-스택)
- [완전한 API 테스트 가이드](#-완전한-api-테스트-가이드)
- [API 엔드포인트 전체 목록](#-api-엔드포인트-전체-목록)
- [데이터베이스 구조](#-데이터베이스-구조)

---

## 📁 프로젝트 구조 (jeonga 브랜치에서 배포됨)

```
tumae-backend/
├── main.py              # 🔥 FastAPI 메인 애플리케이션 파일 
├── render.yaml          # Render 배포 설정 파일
├── requirements.txt     # Python 패키지 의존성 목록
├── runtime.txt          # Python 버전 명시 (3.12.7)
├── start.py             # 서버 시작 스크립트
├── student_data_korea_500.json  # 학생 테스트 데이터 (500개)
└── teacher_data.json    # 튜터 테스트 데이터
```

### 주요 파일

#### `main.py` ⭐ **핵심 파일**

- **API 엔드포인트**: 29개
- **주요 기능**:
  - 인증 시스템 (JWT, Bcrypt)
  - 튜터/학생 프로필 관리
  - 매칭 시스템
  - 커뮤니티 (게시글, 답변, 채택)
  - 쪽지함 시스템
  - 이력서 관리

---

## 🚀 주요 기능

### 1. 회원 인증 시스템
- 학생/튜터 역할 구분 회원가입
- JWT 기반 인증 (Access Token 30분 + Refresh Token 7일)
- Bcrypt 비밀번호 암호화

### 2. 프로필 관리
- 튜터 온보딩 (과목, 시급, 경력, 가능시간 등)
- 학생 온보딩 (희망 과목, 희망 시급 등)
- **⚠️ 중요**: 로그인 활성화를 위해 회원가입 후 반드시 프로필 등록 필요

### 3. 매칭 시스템
- 튜터 목록 조회 및 필터링
- 학생 목록 조회 및 필터링
- 상세 프로필 조회
- 베스트 튜터 랭킹

### 4. 커뮤니티 시스템
- 질문 게시글 작성
- 답변 작성
- 답변 채택 시스템
- 채택 횟수 기반 튜터 랭킹

### 5. 쪽지함 시스템
- 1:1 메시지 송수신
- 읽음/별표 상태 관리
- 답장 체인 연결
- 스레드 조회
- 안읽은 쪽지 개수 확인

### 6. 튜터 이력서 관리
- 4가지 블록 타입 (경력/프로젝트/자격증/포트폴리오)
- CRUD 기능
- 블록별 맞춤 필드 검증

---

## 🛠 기술 스택

### Backend
- **FastAPI 0.104.1** - 고성능 비동기 웹 프레임워크
- **Python 3.12.7** - 최신 안정 버전

### Database
- **PostgreSQL 15+** - 관계형 데이터베이스
- **SQLAlchemy 2.0.23** - ORM 라이브러리
- **Raw SQL** - 복잡한 쿼리 실행

### Security
- **JWT (python-jose)** - 토큰 기반 인증
- **Bcrypt (passlib)** - 비밀번호 암호화

### Deployment
- **Uvicorn** - ASGI 서버
- **Render.com** - 클라우드 배포

---

## 🧪 완전한 API 테스트 가이드

> **중요**: 이 가이드를 순서대로 따라하면 모든 API를 빠짐없이 테스트할 수 있습니다.

### **시작 전 준비**

1. **API 문서 접속**: https://tumae-jeonga.onrender.com/docs
2. **Swagger UI 확인**: 모든 엔드포인트가 카테고리별로 정리되어 있습니다

---

## 📝 테스트 시나리오

### **1️⃣ 인증 시스템 테스트 (Authentication)**

#### **1-1. 학생 회원가입**

**엔드포인트**: `POST /auth/signup`

1. "Try it out" 클릭
2. Request body에 다음 입력:

```json
{
  "name": "김학생",
  "email": "student1@test.com",
  "password": "password123",
  "role": "student",
  "gender": "male",
  "terms_agreed": true,
  "privacy_policy_agreed": true
}
```

3. "Execute" 클릭
4. **응답 확인**: `201 Created` + `user_id` 기억하기 (예: 1234)

#### **1-2. 학생 프로필 등록 (필수!)**

⚠️ **중요**: 프로필을 등록해야 로그인이 활성화됩니다!

**엔드포인트**: `PATCH /auth/students/details`

```json
{
  "user_id": 1234,
  "student_subjects": [1, 2, 3],
  "student_goals": [1, 2],
  "student_lesson_types": [1, 2],
  "student_regions": [1, 2],
  "student_availabilities": [
    {
      "weekday": 0,
      "time_band_id": 1
    },
    {
      "weekday": 2,
      "time_band_id": 2
    }
  ],
  "preferred_price_min": 30000,
  "preferred_price_max": 50000,
  "student_skill_levels": [1, 2],
  "student_age_id": 1
}
```

**필드 설명**:
- `user_id`: 회원가입 시 받은 사용자 ID
- `student_subjects`: 과목 ID 배열 (1=Python, 2=Java 등)
- `student_goals`: 학습 목표 ID 배열
- `student_lesson_types`: 수업 방식 ID 배열 (1=온라인, 2=오프라인)
- `student_regions`: 지역 ID 배열 (1=서울, 2=경기 등)
- `student_availabilities`: 가능 시간대 배열
  - `weekday`: 요일 (0=월요일, 1=화요일, ..., 6=일요일)
  - `time_band_id`: 시간대 ID (1=오전, 2=오후, 3=저녁 등)
- `preferred_price_min`: 최소 희망 시급
- `preferred_price_max`: 최대 희망 시급
- `student_skill_levels`: 실력 수준 ID 배열 (1=초급, 2=중급, 3=고급)
- `student_age_id`: 나이대 ID (1=10대, 2=20대 등)

#### **1-3. 학생 로그인**

**엔드포인트**: `POST /auth/login`

```json
{
  "email": "student1@test.com",
  "password": "password123"
}
```

**응답에서 받을 것**:
```json
{
  "access_token": "eyJhbGc...",
  "refresh_token": "eyJhbGc...",
  "user_id": 1234,
  "role": "student"
}
```

**중요**: `access_token`을 복사하여 메모장에 저장하세요!

⚠️ **이 API는 인증이 없어도 사용 가능**합니다. 대부분의 API는 `user_id`를 Request Body나 Query Parameter로 전달하는 방식입니다.

#### **1-4. 튜터 회원가입**

**엔드포인트**: `POST /auth/signup`

```json
{
  "name": "박튜터",
  "email": "tutor1@test.com",
  "password": "password123",
  "role": "tutor",
  "gender": "female",
  "terms_agreed": true,
  "privacy_policy_agreed": true
}
```

**응답**: `user_id` 기억하기 (예: 1235)

#### **1-5. 튜터 프로필 등록 (필수!)**

**엔드포인트**: `PATCH /auth/tutors/details`

```json
{
  "user_id": 1235,
  "education_level": "bachelor",
  "tutor_subjects": [
    {
      "subject_id": 1,
      "skill_level_id": 3
    },
    {
      "subject_id": 2,
      "skill_level_id": 2
    }
  ],
  "tutor_lesson_types": [1, 2],
  "tutor_availabilities": [
    {
      "weekday": 0,
      "time_band_id": 1
    },
    {
      "weekday": 1,
      "time_band_id": 2
    },
    {
      "weekday": 2,
      "time_band_id": 1
    }
  ],
  "tutor_goals": [1, 2],
  "tutor_skill_levels": [2, 3],
  "hourly_rate_min": 35000,
  "hourly_rate_max": 50000,
  "tutor_regions": [1, 2]
}
```

**필드 설명**:
- `user_id`: 회원가입 시 받은 사용자 ID
- `education_level`: 학력 (예: "bachelor", "master", "doctor")
- `tutor_subjects`: 과목과 실력 수준 배열
  - `subject_id`: 과목 ID (1=Python, 2=Java 등)
  - `skill_level_id`: 해당 과목의 강의 가능 수준 (1=초급, 2=중급, 3=고급)
- `tutor_lesson_types`: 수업 방식 ID 배열 (1=온라인, 2=오프라인)
- `tutor_availabilities`: 가능 시간대 배열
  - `weekday`: 요일 (0=월요일, 1=화요일, ..., 6=일요일)
  - `time_band_id`: 시간대 ID (1=오전, 2=오후, 3=저녁 등)
- `tutor_goals`: 강의 목표 ID 배열
- `tutor_skill_levels`: 강의 가능 수준 ID 배열
- `hourly_rate_min`: 최소 시급
- `hourly_rate_max`: 최대 시급
- `tutor_regions`: 활동 가능 지역 ID 배열

#### **1-6. 튜터 로그인**

**엔드포인트**: `POST /auth/login`

```json
{
  "email": "tutor1@test.com",
  "password": "password123"
}
```

튜터의 `access_token`과 `user_id`도 메모장에 저장해두세요.

#### **1-7. 로그아웃**

**엔드포인트**: `POST /auth/logout`

```json
{
  "user_id": 1234
}
```

#### **1-8. 회원 탈퇴**

**엔드포인트**: `DELETE /auth/users/{user_id}`

- Path parameter에 `user_id` 입력 (예: 1234)
- **주의**: 실제로 삭제되므로 테스트는 나중에!

---

### **2️⃣ 매칭 시스템 테스트**

#### **2-1. 베스트 튜터 랭킹 조회**

**엔드포인트**: `GET /api/tutors/rankings/best`

- Query Parameters:
  - `limit`: 10 (최대 10명)

**설명**: 평점과 경력을 기준으로 상위 튜터 조회

#### **2-2. 튜터 목록 조회 (학생용)**

**엔드포인트**: `GET /api/tutors`

**필터 옵션** (Query Parameters):
- `subjects`: 1 (과목 ID)
- `regions`: 1 (지역 ID)
- `hourly_rate_min`: 30000
- `hourly_rate_max`: 50000
- `available_day`: "monday"
- `skill_levels`: 2
- `lesson_types`: 1
- `goals`: 1
- `sort`: "hourly_rate_asc" (정렬: hourly_rate_asc/desc, rating_desc, career_desc)
- `limit`: 20
- `offset`: 0

**예시 1 - 전체 조회**:
- 파라미터 없이 "Execute"

**예시 2 - 필터 적용**:
```
subjects=1&regions=1&hourly_rate_max=40000&sort=rating_desc
```

#### **2-3. 튜터 상세 조회**

**엔드포인트**: `GET /api/tutors/{tutor_id}`

- Path parameter: `tutor_id` = 1235 (위에서 생성한 튜터 ID)

**응답 내용**:
- 기본 정보 (이름, 소개, 시급, 경력)
- 평점 정보
- 과목, 지역, 가능 시간
- 이력서 블록들

#### **2-4. 학생 목록 조회 (튜터용)**

**엔드포인트**: `GET /api/students`

**필터 옵션**:
- `subjects`: 1
- `regions`: 1
- `skill_levels`: 1
- `lesson_types`: 1
- `goals`: 1
- `limit`: 20
- `offset`: 0

#### **2-5. 학생 상세 조회**

**엔드포인트**: `GET /api/students/{student_id}`

- Path parameter: `student_id` = 1234

---

### **3️⃣ 커뮤니티 시스템 테스트**

#### **3-1. 게시글 작성 (학생/튜터 모두 가능)**

**엔드포인트**: `POST /community/posts`

```json
{
  "author_id": 1234,
  "title": "Python 리스트와 튜플의 차이가 궁금해요",
  "body": "Python을 공부하고 있는데 리스트와 튜플의 차이를 잘 모르겠어요. 자세히 설명해주실 수 있나요?",
  "subject_id": 1,
  "region_id": 1,
  "tags": ["python", "기초", "자료구조"]
}
```

**필드 설명**:
- `author_id`: 작성자의 user_id (학생 또는 튜터)
- `subject_id`: 과목 ID
- `region_id`: 지역 ID (선택사항)
- `tags`: 태그 배열 (선택사항)

**응답**: `post_id` 기억하기 (예: 501)

#### **3-2. 게시글 목록 조회**

**엔드포인트**: `GET /community/posts`

**필터 옵션**:
- `subject_id`: 1
- `region_id`: 1
- `tag`: "python"
- `sort`: "latest" (latest/popular)
- `limit`: 20
- `offset`: 0

**예시 1 - 전체 최신순**:
```
sort=latest&limit=10
```

**예시 2 - Python 태그 필터**:
```
tag=python&sort=popular
```

#### **3-3. 게시글 상세 조회**

**엔드포인트**: `GET /community/posts/{post_id}`

- Path parameter: `post_id` = 501

**응답 내용**:
- 게시글 정보
- 작성자 정보
- 모든 답변 목록
- 채택된 답변 표시

#### **3-4. 답변 작성 (튜터로 진행 권장)**

**엔드포인트**: `POST /community/posts/{post_id}/answers`

**Path Parameter**:
- `post_id` = 501

**Query Parameters**:
- `author_id` = 1235 (튜터의 user_id)
- `body` = 리스트(list)는 대괄호[]로 정의하며 수정 가능(mutable)합니다. 튜플(tuple)은 소괄호()로 정의하며 수정 불가능(immutable)합니다.

**Swagger UI에서 입력 방법**:
1. Path parameter `post_id`에 501 입력
2. Query parameter `author_id`에 1235 입력
3. Query parameter `body`에 답변 내용 입력

**예시 body 내용**:
```
리스트(list)는 대괄호[]로 정의하며 수정 가능(mutable)합니다. 튜플(tuple)은 소괄호()로 정의하며 수정 불가능(immutable)합니다.

예시:
my_list = [1, 2, 3]
my_list[0] = 10  # 가능

my_tuple = (1, 2, 3)
my_tuple[0] = 10  # 에러 발생
```

**응답**: `answer_id` 기억하기 (예: 601)

#### **3-5. 답변 채택 (질문 작성자만 가능)**

**엔드포인트**: `PATCH /community/answers/{answer_id}/accept`

⚠️ **중요**: 질문을 작성한 사용자(1234)의 user_id를 사용해야 합니다.

**Request**:
- Path parameter: `answer_id` = 601

```json
{
  "user_id": 1234
}
```

**효과**:
- 해당 답변이 채택됨
- 튜터의 `accepted_count` 증가 (랭킹 상승)
- 다른 답변의 채택은 자동 취소

---

### **4️⃣ 쪽지함 시스템 테스트**

#### **4-1. 쪽지 보내기**

**엔드포인트**: `POST /messages/send`

**Query Parameter**: 
- `sender_id`: 1234 (발신자의 user_id)

**시나리오**: 학생(1234)이 튜터(1235)에게 쪽지 보내기

```json
{
  "receiver_id": 1235,
  "subject": "Python 과외 문의드립니다",
  "body": "안녕하세요. Python 기초부터 차근차근 배우고 싶은데 과외 가능하신가요?",
  "reply_to": null
}
```

**필드 설명**:
- `receiver_id`: 수신자의 user_id
- `subject`: 쪽지 제목
- `body`: 쪽지 내용
- `reply_to`: 답장할 쪽지의 ID (새 쪽지면 null)

**응답**: `message_id` 기억하기 (예: 701)

#### **4-2. 받은 쪽지함 조회**

**엔드포인트**: `GET /messages/inbox`

**Query Parameter**: 
- `user_id` = 1235 (튜터)

**응답**: 튜터가 받은 모든 쪽지 목록

#### **4-3. 보낸 쪽지함 조회**

**엔드포인트**: `GET /messages/sent`

**Query Parameter**:
- `user_id` = 1234 (학생)

**응답**: 학생이 보낸 모든 쪽지 목록

#### **4-4. 쪽지 상세 조회**

**엔드포인트**: `GET /messages/{message_id}`

- Path parameter: `message_id` = 701
- Query parameter: `user_id` = 1235 (튜터)

**자동 처리**: 
- 수신자가 조회하면 `is_read`가 자동으로 `true`로 변경

#### **4-5. 답장 보내기**

**엔드포인트**: `POST /messages/send`

**Query Parameter**: 
- `sender_id`: 1235 (튜터)

**시나리오**: 튜터(1235)가 학생(1234)에게 답장

```json
{
  "receiver_id": 1234,
  "subject": "Re: Python 과외 문의드립니다",
  "body": "안녕하세요! 네, 과외 가능합니다. 주 2회 온라인 수업으로 진행 가능한데 어떠신가요?",
  "reply_to": 701
}
```

**응답**: 새 `message_id` (예: 702)

#### **4-6. 쪽지 스레드 조회**

**엔드포인트**: `GET /messages/{message_id}/thread`

- Path parameter: `message_id` = 701
- Query parameter: `user_id` = 1234

**응답**: 701번 쪽지와 관련된 모든 답장 목록 (스레드)

#### **4-7. 별표 토글**

**엔드포인트**: `PATCH /messages/{message_id}/star`

- Path parameter: `message_id` = 701

```json
{
  "user_id": 1234
}
```

**효과**: 별표(중요 표시) ON/OFF 토글

#### **4-8. 안읽은 쪽지 개수**

**엔드포인트**: `GET /messages/unread/count`

- Query parameter: `user_id` = 1235

**응답**:
```json
{
  "user_id": 1235,
  "unread_count": 0
}
```

#### **4-9. 쪽지 삭제**

**엔드포인트**: `DELETE /messages/{message_id}`

- Path parameter: `message_id` = 701

```json
{
  "user_id": 1234,
  "role": "sender"
}
```

**설명**:
- `role`: "sender" 또는 "receiver"
- 발신자 삭제: `sender_deleted = true`
- 수신자 삭제: `receiver_deleted = true`
- 양쪽 모두 삭제하면 DB에서 완전 삭제

---

### **5️⃣ 이력서 시스템 테스트 (튜터 전용)**

#### **5-1. 이력서 전체 조회**

**엔드포인트**: `GET /resume/{tutor_id}`

- Path parameter: `tutor_id` = 1235

**응답**: 4가지 블록 타입별로 정리된 이력서
```json
{
  "tutor_id": 1235,
  "career": [...],
  "project": [...],
  "certificate": [...],
  "portfolio": [...]
}
```

#### **5-2. 경력 블록 추가**

**엔드포인트**: `POST /resume/{tutor_id}`

- Path parameter: `tutor_id` = 1235

⚠️ **중요**: 이 API는 **Form Data** 형식입니다. Swagger UI에서 각 필드를 개별적으로 입력해야 합니다.

**Form Data 입력**:
- `block_type`: career
- `title`: 네이버 백엔드 개발자
- `period`: 2020.03 ~ 2023.02
- `role`: Backend Developer
- `description`: Python/Django 기반 API 서버 개발 및 운영
- `tech_stack`: Python, Django, PostgreSQL, Redis
- `file_url`: (비워두기)
- `link_url`: (비워두기)
- `issuer`: (비워두기)
- `acquired_at`: (비워두기)

**응답**: `block_id` 기억하기 (예: 801)

#### **5-3. 프로젝트 블록 추가**

**엔드포인트**: `POST /resume/{tutor_id}`

**Form Data 입력**:
- `block_type`: project
- `title`: AI 챗봇 시스템 개발
- `period`: 2022.06 ~ 2022.12
- `role`: Lead Developer
- `description`: FastAPI + LangChain을 활용한 고객 상담 챗봇 개발
- `tech_stack`: FastAPI, LangChain, OpenAI API
- `link_url`: https://github.com/example/chatbot
- `file_url`: (비워두기)
- `issuer`: (비워두기)
- `acquired_at`: (비워두기)

#### **5-4. 자격증 블록 추가**

**엔드포인트**: `POST /resume/{tutor_id}`

**Form Data 입력**:
- `block_type`: certificate
- `title`: 정보처리기사
- `issuer`: 한국산업인력공단
- `acquired_at`: 2021-08-20
- `file_url`: https://example.com/cert.pdf
- `period`: (비워두기)
- `role`: (비워두기)
- `description`: (비워두기)
- `tech_stack`: (비워두기)
- `link_url`: (비워두기)

#### **5-5. 포트폴리오 블록 추가**

**엔드포인트**: `POST /resume/{tutor_id}`

**Form Data 입력**:
- `block_type`: portfolio
- `title`: 개인 블로그 - Python 강의 자료
- `description`: Python 초급부터 고급까지 체계적인 강의 자료 300개 이상
- `tech_stack`: Python, Jupyter Notebook
- `link_url`: https://blog.example.com
- `file_url`: (비워두기)
- `period`: (비워두기)
- `role`: (비워두기)
- `issuer`: (비워두기)
- `acquired_at`: (비워두기)

#### **5-6. 이력서 블록 수정**

**엔드포인트**: `PATCH /resume/block/{block_id}`

- Path parameter: `block_id` = 801

**Request Body** (JSON):
```json
{
  "title": "네이버 시니어 백엔드 개발자",
  "period": "2020.03 ~ 2024.02",
  "role": "Senior Backend Developer",
  "description": "Python/Django/FastAPI 기반 API 서버 개발, 팀 리딩 경험",
  "tech_stack": "Python, Django, FastAPI, PostgreSQL, Redis, Docker"
}
```

**참고**: 수정하고 싶은 필드만 포함하면 됩니다. 나머지 필드는 그대로 유지됩니다.

#### **5-7. 이력서 블록 삭제**

**엔드포인트**: `DELETE /resume/block/{block_id}`

- Path parameter: `block_id` = 801
- Query parameter: `current_user_id` = 1235 (튜터의 user_id)

---

### **6️⃣ 기타 엔드포인트**

#### **6-1. 루트 엔드포인트**

**엔드포인트**: `GET /`

**응답**:
```json
{
  "message": "Tumae API is running",
  "version": "4.3.0"
}
```

---

## 📊 API 엔드포인트 전체 목록

### **인증 (Authentication) - 6개**
| 메서드 | 엔드포인트 | 설명 | user_id 필요 |
|--------|-----------|------|-------------|
| POST | `/auth/signup` | 회원가입 | ❌ |
| POST | `/auth/login` | 로그인 | ❌ |
| POST | `/auth/logout` | 로그아웃 | ✅ |
| DELETE | `/auth/users/{user_id}` | 회원 탈퇴 | ✅ (path) |
| PATCH | `/auth/tutors/details` | 튜터 프로필 등록 | ✅ (body) |
| PATCH | `/auth/students/details` | 학생 프로필 등록 | ✅ (body) |

### **매칭 (Matching) - 5개**
| 메서드 | 엔드포인트 | 설명 | user_id 필요 |
|--------|-----------|------|-------------|
| GET | `/api/tutors/rankings/best` | 베스트 튜터 랭킹 | ❌ |
| GET | `/api/tutors` | 튜터 목록 조회 | ❌ |
| GET | `/api/tutors/{tutor_id}` | 튜터 상세 조회 | ❌ |
| GET | `/api/students` | 학생 목록 조회 | ❌ |
| GET | `/api/students/{student_id}` | 학생 상세 조회 | ❌ |

### **커뮤니티 (Community) - 5개**
| 메서드 | 엔드포인트 | 설명 | user_id 필요 |
|--------|-----------|------|-------------|
| POST | `/community/posts` | 게시글 작성 | ✅ (body - author_id) |
| GET | `/community/posts` | 게시글 목록 | ❌ |
| GET | `/community/posts/{post_id}` | 게시글 상세 | ❌ |
| POST | `/community/posts/{post_id}/answers` | 답변 작성 | ✅ (query - author_id, body) |
| PATCH | `/community/answers/{answer_id}/accept` | 답변 채택 | ✅ (body) |

### **쪽지함 (Messages) - 8개**
| 메서드 | 엔드포인트 | 설명 | user_id 필요 |
|--------|-----------|------|-------------|
| POST | `/messages/send` | 쪽지 보내기 | ✅ (query - sender_id) |
| GET | `/messages/inbox` | 받은 쪽지함 | ✅ (query) |
| GET | `/messages/sent` | 보낸 쪽지함 | ✅ (query) |
| GET | `/messages/{message_id}` | 쪽지 상세 | ✅ (query) |
| GET | `/messages/{message_id}/thread` | 쪽지 스레드 | ✅ (query) |
| DELETE | `/messages/{message_id}` | 쪽지 삭제 | ✅ (query) |
| PATCH | `/messages/{message_id}/star` | 별표 토글 | ✅ (query) |
| GET | `/messages/unread/count` | 안읽은 개수 | ✅ (query) |

### **이력서 (Resume) - 4개**
| 메서드 | 엔드포인트 | 설명 | user_id 필요 |
|--------|-----------|------|-------------|
| GET | `/resume/{tutor_id}` | 이력서 조회 | ❌ |
| POST | `/resume/{tutor_id}` | 이력서 블록 추가 | ✅ (path - tutor_id) |
| PATCH | `/resume/block/{block_id}` | 이력서 블록 수정 | ❌ (자동 검증) |
| DELETE | `/resume/block/{block_id}` | 이력서 블록 삭제 | ✅ (query - current_user_id) |

### **기타 - 1개**
| 메서드 | 엔드포인트 | 설명 | user_id 필요 |
|--------|-----------|------|-------------|
| GET | `/` | 루트 (헬스체크) | ❌ |

**총 29개 API 엔드포인트**

---

## 🗄 데이터베이스 구조

### 주요 테이블

#### **users**
회원 기본 정보
- id, name, email, password_hash, role, gender, signup_status, created_at

#### **tutor_profiles / student_profiles**
튜터/학생 상세 프로필
- introduction, career_years, hourly_rate, rating_avg, accepted_count 등

#### **posts / answers**
커뮤니티 게시글 및 답변
- is_accepted 컬럼으로 채택 관리
- accepted_count로 튜터 랭킹 산정

#### **messages**
쪽지 데이터
- is_read, is_starred, reply_to
- sender_deleted, receiver_deleted (양방향 삭제)

#### **resume_blocks**
튜터 이력서 (4가지 블록 타입)
- career, project, certificate, portfolio

#### **관계 테이블**
- tutor_subjects, student_subjects
- tutor_regions, student_regions
- tutor_lesson_types, student_lesson_types
- post_tags

---

## 🎯 테스트 체크리스트

### **필수 테스트**
- [ ] 학생 회원가입 → 프로필 등록 → 로그인
- [ ] 튜터 회원가입 → 프로필 등록 → 로그인
- [ ] 튜터 목록 조회 및 필터링
- [ ] 게시글 작성 → 답변 작성 → 답변 채택
- [ ] 쪽지 보내기 → 받은 쪽지함 확인 → 답장
- [ ] 이력서 4가지 블록 타입 모두 추가

### **고급 테스트**
- [ ] 베스트 튜터 랭킹 조회
- [ ] 쪽지 스레드 조회
- [ ] 안읽은 쪽지 개수 확인
- [ ] 별표 기능 테스트
- [ ] 이력서 블록 수정/삭제
- [ ] 회원 탈퇴

---

## 💡 테스트 팁

### **1. user_id 관리**
- 학생과 튜터의 user_id를 메모장에 따로 저장해두세요
- 대부분의 API는 user_id를 Request Body나 Query Parameter로 전달합니다

### **2. 다양한 ID 저장**
- 생성된 user_id, post_id, message_id, block_id를 메모하세요
- 나중에 조회/수정/삭제 시 필요합니다

### **3. 순서 지키기**
- 회원가입 → 프로필 등록 → 로그인 순서는 필수!
- 프로필 등록 없이 로그인하면 제한될 수 있습니다

### **4. 역할 구분**
- 답변 작성은 튜터로 (tutor user_id 사용)
- 답변 채택은 질문 작성자로 (student user_id 사용)
- 각 API에서 올바른 user_id를 사용하세요

### **5. Form Data vs JSON**
- 이력서 블록 추가: **Form Data** 형식
- 이력서 블록 수정: **JSON** 형식
- Swagger UI에서 자동으로 적절한 입력 형식을 제공합니다

### **6. 에러 확인**
- 에러 응답의 `detail` 메시지를 확인하세요
- 대부분 필수 필드 누락이나 권한 문제입니다

---

## 🐛 자주 발생하는 오류

### **1. "USER_NOT_FOUND" 또는 "INVALID_USER_OR_ROLE"**
**원인**: 잘못된 user_id 또는 role
**해결**: 회원가입/로그인 응답에서 받은 정확한 user_id 사용

### **2. "PROFILE_NOT_COMPLETE"**
**원인**: 프로필 등록을 하지 않음
**해결**: PATCH /auth/tutors/details 또는 /auth/students/details 실행

### **3. "FORBIDDEN" 또는 403 오류**
**원인**: 권한 없음 (예: 학생이 튜터 전용 API 호출, 또는 다른 사람의 리소스 수정 시도)
**해결**: 올바른 역할의 user_id 사용

### **4. 422 Unprocessable Entity**
**원인**: 필수 필드 누락 또는 잘못된 데이터 타입
**해결**: Request body의 모든 필드 확인, 특히 배열 형식 주의

### **5. "TUTOR_NOT_FOUND" 또는 "STUDENT_NOT_FOUND"**
**원인**: 존재하지 않는 user_id 또는 프로필 미등록
**해결**: 프로필 등록 확인, 올바른 user_id 사용

### **6. Form Data 입력 오류 (이력서 추가 시)**
**원인**: JSON 형식으로 입력 시도
**해결**: Swagger UI에서 각 필드를 개별적으로 입력 (Form Data 방식)


**프로젝트 상태**: ✅ 정상 운영 중 | 마지막 업데이트: 2025년 12월
